{% extends 'HRUserBundle::layout.html.twig' %}

{% block user_content %}
    {{ form(form, {'action': path('position_edit', {'positionId': position.id}), 'attr': {'class': 'form-horizontal' }}) }}
{% endblock %}